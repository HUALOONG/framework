# Jowen Framework 未完成项修复计划（2026-09-04）

> 依据 `docs/unfinished-work-inventory-2026-09-04.md`（14 项）逐条定位 + 制定修复方案。
> 本文件为分析与计划；实际代码变更记录见 `docs/change-summary-2026-09-04.md`。
> 标注 🔴=需决策/受阻塞，🟡=待依赖，🟢=可立即或已修复，⚪=by-design（无需改代码，仅补文档）。

---

## 0. 一手核实后的关键事实（修正文档基线）

1. **P0-002 范围被低估**：文档描述为"data-mybatis 11 文件未提交"，实际 `git status` 显示 **framework-data-core / data-jdbc / data-mybatis 三模块约 200 文件未提交删除+重写**（query、repository、transaction 整包删除；Flex* 系列删除；ExtensionRegistry 重写）。这是主流程硬阻塞，且范围远大于原描述。
2. **P1-002 实为 by-design**：`PluginManager.initialize`（L45）与 `PluginLifecycleManager.loadPlugins/loadPlugin`（L180/185）抛 `UnsupportedOperationException`，但错误消息明确引导"请使用 PluginLoader / PluginLifecycleManager"；且 `PluginLifecycleManagerTest.java:180,182` **显式断言抛此异常**。移除会破坏既有契约与测试。
3. **P1-003 实为 by-design**：`FlexEncryptProcessor.decrypt(String)`（L133–145）**已实现**（AES/ECB/PKCS5Padding）；仅嵌套 `FieldEncryptor.decrypt` 默认抛 USO，且 `FlexEncryptProcessorTest.java:103` **显式断言该默认行为**。属扩展点占位，非功能缺失。文档称"内嵌 Decrypt 接口"与代码（FieldEncryptor）命名略有出入。
4. **P2-002 分布确认**：全仓 6 个 `RuntimeHintsRegistrar`（`Cache/I18n/Jdbc/Logger/Mybatis/Spi`）全部集中在 `framework-boot-autoconfigure/.../runtime/`，core/extras/plugin 无独立 registrar。
5. **P2-001 确认 7 个 sender**：`provider/` 下 Email/DingTalk/Push/Site/Sms/Webhook/WeCom 均为 abstract 骨架（模板方法），与文档一致。

---

## 1. P0-001 自动配置漏登记 ✅ 已修复

- **问题与影响**：唯一 `AutoConfiguration.imports` 仅登记 11 个 `@AutoConfiguration`，漏登 `FileStorageAutoConfiguration` / `MessageAutoConfiguration` / `WebExtrasAutoConfiguration`。无 `spring.factories`、无第二 imports 文件、生产代码零引用——此前仅测试用 `AutoConfigurations.of(...)` 显式加载。导致文件存储、消息、分布式 Web 增强 3 组能力在生产环境静默失效（覆盖 95% 仍绿，典型测试盲区）。
- **位置**：`framework-boot/framework-boot-autoconfigure/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`（已核实 3 类均带 `@AutoConfiguration`，包 `cn.jowen.framework.boot.autoconfigure.extras`）。
- **修复步骤**：在 `ExtrasAutoConfiguration` 行后补登 3 行（已完成）。
- **副作用/兼容性**：纯增量；3 类均带 `@ConditionalOnProperty`/`@ConditionalOnClass` 守卫，按属性开关启用。现有测试不受影响。
- **建议补强**：新增"从 imports 资源全量加载装配类"元测试（防回归，对应测试缺口表 B 的 P0 项）。⚪ 可选。

---

## 2. P0-002 data 层跨模块重构提交 🔴 需决策

- **问题与影响**：约 200 文件未提交删除+重写，主流程推进的硬阻塞。若长期不提交，后续所有 data 相关修复（P1-005/006、P2-003）都会与未提交变更冲突/返工。
- **位置**：`framework-data/framework-data-core`（query/repository/transaction 整包删除）、`framework-data/framework-data-jdbc`（repository/transaction 删除）、`framework-data/framework-data-mybatis`（Flex* 删除、ExtensionRegistry 重写、新增 Encrypted 注解）。
- **修复步骤（待确认）**：
  1. 确认重构目标形态：是否彻底转向 MyBatis Flex 原生 API（旧审计 P0-C-①②③ 自动消失），还是保留更小的自研 Repository 抽象？
  2. 决定提交粒度：整批单 commit，或按 core→jdbc→mybatis 分模块提交。
  3. 提交前运行全量 reactor 构建 + JaCoCo 门禁回归（目标 ≥0.95）。
- **副作用/兼容性**：提交本身是安全的（工作树当前据文档为 95.26% 覆盖、19/19 模块 BUILD SUCCESS）；风险在于若重构未完全收敛（如某删除类的调用方未清理），提交后编译会失败——需先 `mvn -o install` 验证。
- **状态**：🔴 待你确认提交策略与目标形态后再执行。

---

## 3. P1-001 跨节点缓存同步 🔴 需选型

- **问题与影响**：`CacheSyncListener` 仅定义接口（`onCachePut`/`onCacheEvict`），全仓无实现。多级缓存的跨节点一致性缺失，与 README §10.3 "命中率可观测/一致性" 存在落差。
- **位置**：`framework-cache/src/main/java/cn/jowen/framework/cache/cache/multilevel/CacheSyncListener.java`。
- **修复步骤（待选型）**：
  1. 选定底座：Redis pub/sub 或 Redisson topic（或 Redis Sentinel 事件）。
  2. 新增 `RedisCacheSyncListener implements CacheSyncListener`，订阅频道/主题，收到事件后调用本地 `MultilevelCache` 的 put/evict。
  3. 在 `CacheAutoConfiguration` 中按 `spring.cache.multilevel.sync.enabled` 条件装配；提供本地无 Redis 时的 no-op 默认实现保持向后兼容。
  4. 配套单测（测试缺口表 B 的 P1）。
- **副作用/兼容性**：新增可选能力，默认关闭不破坏现有行为；引入 Redis/Redisson 依赖需确认已在 BOM 纳管。🔴 选型决定实现与测试策略。
- **状态**：🔴 待你确认底座选型。

---

## 4. P1-002 插件生命周期 ⚪ by-design（无需改代码）

- **问题与影响**：3 处 `UnsupportedOperationException`（`PluginManager.initialize` L45、`PluginLifecycleManager.loadPlugins` L180、`loadPlugin` L185）。文档列为"对外契约未完成"。
- **位置**：`framework-plugin/.../api/PluginManager.java:45`、`.../lifecycle/PluginLifecycleManager.java:180,185`。
- **分析**：错误消息明确引导至 `PluginLoader` / `PluginLifecycleManager`；且 `PluginLifecycleManagerTest.java:180,182` **显式断言抛异常**。属契约引导性设计，非缺陷。
- **修复步骤（建议）**：仅补 javadoc/README 说明"请使用 PluginLoader 加载后调 initialize"，消除"接口割裂"观感；**不修改抛异常逻辑**（否则破坏既有测试）。
- **副作用/兼容性**：纯文档，无代码风险。
- **状态**：⚪ 需你确认"仅补文档"而非改实现。

---

## 5. P1-003 加解密策略 ⚪ 已落地（仅需补文档）

- **问题与影响**：文档称 `Decrypt` 接口 default 抛 USO 需策略类补齐。
- **位置**：`framework-data/framework-data-mybatis/.../extension/FlexEncryptProcessor.java:163`（嵌套 `FieldEncryptor.decrypt` 默认抛 USO）。
- **分析**：`FlexEncryptProcessor.decrypt(String)`（L133–145）**已实现 AES/ECB**；仅 `FieldEncryptor` 扩展点默认抛 USO，且 `FlexEncryptProcessorTest.java:103` 显式断言该默认行为。Processor 透明加解密链路完整。
- **修复步骤（建议）**：如需对外提供自定义字段加解密策略，可新增 `FieldEncryptor` 的具体实现类（如 `AesFieldEncryptor`）并注册；否则仅补 javadoc 说明该默认抛出的意图。
- **副作用/兼容性**：新增策略类为纯增量；不改动现有默认行为。
- **状态**：⚪ 需你确认是否要提供具体策略类，或仅补文档。

---

## 6. P1-004 分布式 Web 增强 🟡 待 P1-001

- **问题与影响**：`WebExtrasAutoConfiguration.cryptoProcessor()`（L140）用 `new HashMap<String,byte[]>()` 存密钥；幂等/限流/验证码存储为单实例，多实例部署无法跨节点生效。与 README §10.3 分布式承诺落差。
- **位置**：`framework-boot/framework-boot-autoconfigure/.../extras/WebExtrasAutoConfiguration.java:140`（及 IdempotentStore/CaptchaStore 等单实例存储）。
- **修复步骤（待 P1-001）**：将幂等/限流/验证码/密钥存储后端切换为共享存储（Redis），复用 CacheSyncListener 底座或独立 RedisTemplate；保留本地实现作为单实例默认。
- **副作用/兼容性**：多实例下行为改变（跨节点共享），单实例默认行为不变；需确认 Redis 依赖可用。🟡 依赖 P1-001 底座决策。
- **状态**：🟡 待 P1-001 完成。

---

## 7. P1-005 事务 NESTED 传播 🔴 需确认是否必要

- **问题与影响**：`framework-data-jdbc` transaction 目录无 `NESTED`/`SAVEPOINT` 匹配——无保存点回滚能力。
- **位置**：`framework-data/framework-data-jdbc/src/main/java/.../transaction/`（已核实无 NESTED/SAVEPOINT）。
- **分析**：需确认框架事务契约——若约定仅支持 REQUIRED/REQUIRES_NEW（Spring 默认子集），则属文档漂移而非功能缺失；若承诺 NESTED，则需实现。
- **修复步骤（待确认）**：若必要 → 基于 JDBC `Connection.setSavepoint` 实现 NESTED，配套 TDD 单测；若不必要 → 收敛 README 表述。
- **副作用/兼容性**：新增传播级别为增量；需确认当前事务管理器（`TransactionSynchronizationManager` 已改）支持保存点。🔴 待你确认必要性。
- **状态**：🔴 待确认。

---

## 8. P1-006 SQL 联表查询 🟡 待 P2-003

- **问题与影响**：`framework-data-jdbc` 无 `join`/`JoinType` 匹配，缺联表能力；README §8.4 声称的能力存在落差。
- **位置**：`framework-data/framework-data-jdbc/src/main/java/.../repository/`（已核实无 join）。
- **分析**：且 `data-core` 的 `QueryWrapper` 已被删除（P2-003），实现路径依赖重构方向。
- **修复步骤（待 P2-003）**：重构明确后，在 data-jdbc 提供 `JoinSpec`/`JoinType` 与 QueryWrapper 集成，生成 JOIN SQL + 结果映射。
- **副作用/兼容性**：新增查询能力；需与 P2-003 的 QueryWrapper 形态对齐。🟡 依赖 P2-003。
- **状态**：🟡 待 P2-003。

---

## 9. P2-001 消息通道 ⚪ by-design（模板方法）

- **问题与影响**：7 个 sender（Email/DingTalk/Push/Site/Sms/Webhook/WeCom）为 41~57 行 abstract 骨架，`doSend` 待下游实现。README 称"开箱即用"有落差。
- **位置**：`framework-extras/framework-extras-message/src/main/java/.../provider/*MessageSender.java` ×7。
- **分析**：模板方法模式，abstract 骨架本身即设计意图；"开箱即用"表述需收敛为"提供可继承基类"。
- **修复步骤（建议）**：可选提供 1~2 个具体实现样例（如 Webhook via RestTemplate、Site 内存投递）；或将 README 表述改为"可继承基类，需注入实际通道客户端"。
- **副作用/兼容性**：新增样例为增量；不改动骨架。
- **状态**：⚪ 需你确认是否要补样例实现，或仅改 README。

---

## 10. P2-002 GraalVM AOT RuntimeHints 🟢 可立即（较大但明确）

- **问题与影响**：仅 boot-autoconfigure 有 6 个 `RuntimeHintsRegistrar`（Cache/I18n/Jdbc/Logger/Mybatis/Spi），core/extras/plugin 无独立 registrar。README §5/§8 要求每模块提供，未落地。
- **位置**：`framework-boot/framework-boot-autoconfigure/src/main/java/.../runtime/` 现有 6 个；需在 `framework-core`/`framework-extras*`/`framework-plugin` 补 registrar 并在各自 `META-INF/spring/aot.factories` 登记。
- **修复步骤**：为每个缺少的模块新增 `*RuntimeHints implements RuntimeHintsRegistrar`，注册反射/资源/序列化提示（如 plugin 的 SPI 加载类、extras 的 message provider、core 的 ExtensionLoader 扫描类）；在模块 `src/main/resources/META-INF/spring/aot.factories` 登记 `org.springframework.aot.hint.RuntimeHintsRegistrar=...`。
- **副作用/兼容性**：仅影响 GraalVM 原生镜像构建，不影响 JVM 运行；需保证 registrar 不引入额外强制依赖。
- **状态**：🟢 可立即执行（建议放在 P0-002 提交后，避免与 data 重构重叠）。

---

## 11. P2-003 QueryWrapper 重构 🔴 需确认目标形态

- **问题与影响**：`framework-data-core/.../query/QueryWrapper.java` 已删除（HEAD 存在），且无替代类。旧审计指出 `having()` 为伪实现。重构方向未明，阻塞 P1-006。
- **位置**：`framework-data/framework-data-core/src/main/java/.../query/`（整包已删）。
- **修复步骤（待确认）**：确认目标形态——(a) 彻底改用 MyBatis Flex `QueryWrapper` 原生 API；(b) 保留更小的自研 `QueryWrapper` 并重写 `having()` 为独立 HAVING 子句。据此补齐并补测试。
- **副作用/兼容性**：影响所有依赖 query 包的调用方（当前已随 P0-002 重构删除其调用方）。🔴 待你确认形态（建议与 P0-002 一并决策）。
- **状态**：🔴 待确认（并入 P0-002 决策）。

---

## 12. P2-004 文档一致性 🟢 可立即（建议收尾阶段统一做）

- **问题与影响**：① README §8.9 称 extras 含 4 子模块（实际 3，web 在 boot-web）；② boot-web README 标题仍 "framework-extras-web" 且误称"基于 framework-cache 存储底座"；③ extras-message README §4.2 列 4 发送器（实际 7）；④ code-analysis.md 行号/EventBus 行为/7 大风险状态全部过时。
- **位置**：各模块 `README.md`、`docs/code-analysis.md`。
- **修复步骤**：以修复后代码为准，一次性对齐模块数、发送器数、标题、存储底座描述、行号引用与风险状态；已有一轮 08-28 清理（i18n Redis/AOT/指标）。
- **副作用/兼容性**：纯文档，零风险。
- **状态**：🟢 建议在代码修复收尾阶段统一执行（避免反复）。

---

## 3. 建议执行顺序（修订版）

```
第 1 步  P0-001 ✅ 已完成（3 行 imports）
   │
第 2 步  🔴 P0-002 提交决策（你确认策略/形态后，我执行提交+全量回归）
   │      └─ 同步解决 P2-003（QueryWrapper 形态）
   │
第 3 步（并行两线）
   ├─ 线① 分布式：🔴 P1-001 选型 → 🟡 P1-004 WebExtras 分布式化
   └─ 线② 事务/查询：🔴 P1-005 必要性确认 → 🟡 P1-006 join（依赖 P2-003）
   │
第 4 步  ⚪ P1-002 / P1-003 仅补文档（确认后）
   │
第 5 步  🟢 P2-002 RuntimeHints 各模块拆分
   │
第 6 步  ⚪ P2-001 message 样例/README 收敛（确认后）
   │
第 7 步  🟢 P2-004 文档统一修正
```

**关键依赖**：P0-002 → 一切 data 相关 P1/P2-003；P1-001 → P1-004；P2-003 → P1-006。
**明确非缺陷（by-design）**：P1-002、P1-003（均有测试断言其抛 USO），其"修复"仅为文档澄清，不可改实现。
