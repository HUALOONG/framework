<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.jowen.framework</groupId>
        <artifactId>framework-boot</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>framework-boot-starter</artifactId>
    <packaging>pom</packaging>
    <description>框架 Starter 聚合入口，纯 POM 无代码，引入单一依赖即获 core/logger/autoconfigure 默认能力，data/cache/i18n 按需另行引入</description>

    <dependencies>
        <dependency>
            <groupId>cn.jowen.framework</groupId>
            <artifactId>framework-core</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.jowen.framework</groupId>
            <artifactId>framework-logger</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.jowen.framework</groupId>
            <artifactId>framework-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Spring Boot 运行时（默认 Logback） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
    </dependencies>
</project>
